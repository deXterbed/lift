[project]
name = "lift"
version = "0.1.0"
description = "Lift — auto-copy on text selection (macOS)"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyobjc-framework-Quartz>=10.0",
]

[project.optional-dependencies]
dev = [
    "py2app>=0.28",
]
